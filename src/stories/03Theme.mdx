import { Meta } from "@storybook/addon-docs/blocks";

export const RightArrow = () => <svg 
    viewBox="0 0 14 14" 
    width="8px" 
    height="14px" 
    style={{ 
      marginLeft: '4px',
      display: 'inline-block',
      shapeRendering: 'inherit',
      verticalAlign: 'middle',
      fill: 'currentColor',
      'path fill': 'currentColor'
    }}
>
  <path d="m11.1 7.35-5.5 5.5a.5.5 0 0 1-.7-.7L10.04 7 4.9 1.85a.5.5 0 1 1 .7-.7l5.5 5.5c.2.2.2.5 0 .7Z" />
</svg>

<Meta title="Documentation/03. Theme" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Theme Configuration

    Customize every aspect of AtomicFlow to match your brand and design requirements with a simple configuration file.
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">🎨 Overview</h3>
      <p className="sb-section-item-paragraph">
        AtomicFlow uses a theme-first architecture where all design decisions are controlled through a single configuration file. This approach ensures consistency across your application while providing maximum flexibility.
      </p>
      <div style={{
        background: '#e7f3ff',
        border: '1px solid #b8daff',
        borderRadius: '8px',
        padding: '16px',
        margin: '16px 0'
      }}>
        <strong>💡 Hot Reload:</strong> Changes to your theme configuration automatically update your components in real-time during development!
      </div>
    </div>
  </div>
<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">⚙️ Basic Configuration</h3>
      <p className="sb-section-item-paragraph">
        Create an <code>atomicflow.config.js</code> file in your project root:
      </p>
      ```tsx
      // atomicflow.config.tsx
      import type { ThemeConfig } from 'atomicflow';

      const config: ThemeConfig = {
        primaryColor: 'blue',
        neutralColor: 'slate',
        defaultRadius: 'md',
        defaultAppearance: 'system',
        panelBg: 'solid',
        fontHeading: '"Inter", sans-serif',
        fontBody: '"Inter", sans-serif'
      };

      export default config;
      ```
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">🎯 Primary Color</h3>
      <p className="sb-section-item-paragraph">
        The primary color is used for buttons, links, focus states, and other interactive elements. You have two options:
      </p>
      
      <div style={{ marginBottom: '24px' }}>
        <strong>Option 1: Tailwind Color Names</strong>
        ```tsx
        primaryColor: 'emerald'  // Uses Tailwind's emerald color scale
        ```
        <div style={{ fontSize: '14px', margin: '8px 0' }}>
          Available colors: <code>slate</code>, <code>gray</code>, <code>zinc</code>, <code>neutral</code>, <code>stone</code>, <code>red</code>, <code>orange</code>, <code>amber</code>, <code>yellow</code>, <code>lime</code>, <code>green</code>, <code>emerald</code>, <code>teal</code>, <code>cyan</code>, <code>sky</code>, <code>blue</code>, <code>indigo</code>, <code>violet</code>, <code>purple</code>, <code>fuchsia</code>, <code>pink</code>, <code>rose</code>
        </div>
      </div>

      <div>
        <strong>Option 2: Custom Color Scale</strong>
        ```tsx
        primaryColor: {
          50: '#f0f9ff',   // Lightest shade
          100: '#e0f2fe',
          200: '#bae6fd',
          300: '#7dd3fc',
          400: '#38bdf8',
          500: '#0ea5e9',  // Main brand color
          600: '#0284c7',
          700: '#0369a1',
          800: '#075985',
          900: '#0c4a6e',
          950: '#082f49'   // Darkest shade
        }
        ```
        <div style={{
          background: '#fff3cd',
          border: '1px solid #ffeaa7',
          borderRadius: '8px',
          padding: '16px',
          margin: '16px 0'
        }}>
          <strong>⚠️ Important:</strong> When using custom colors, you must provide ALL 11 shades (50-950) to ensure proper theming across light and dark modes.
        </div>
      </div>
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">⚪ Neutral Color</h3>
      <p className="sb-section-item-paragraph">
        Neutral colors are used for text, backgrounds, borders, and other non-interactive elements:
      </p>
      ```tsx
      // Tailwind neutral colors
      neutralColor: 'slate'  // Options: slate, gray, zinc, neutral, stone

      // Or custom neutral scale (same format as primaryColor)
      neutralColor: {
        50: '#fafafa',
        100: '#f4f4f5',
        // ... all shades 50-950
        950: '#09090b'
      }
      ```
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">📐 Border Radius</h3>
      <p className="sb-section-item-paragraph">
        Control the roundness of buttons, cards, inputs, and other components:
      </p>
      ```tsx
      defaultRadius: 'lg'  // Applied to components like Button, Card, Input, etc.
      ```
      <div style={{ marginTop: '16px' }}>
        <strong>Available options:</strong>
        <ul style={{ marginTop: '8px' }}>
          <li><code>none</code> - 0px (sharp corners)</li>
          <li><code>xs</code> - 2px</li>
          <li><code>sm</code> - 4px</li>
          <li><code>md</code> - 6px (default)</li>
          <li><code>lg</code> - 8px</li>
          <li><code>xl</code> - 12px</li>
          <li><code>2xl</code> - 16px</li>
          <li><code>3xl</code> - 24px</li>
          <li><code>4xl</code> - 32px</li>
          <li><code>full</code> - Fully rounded (pills)</li>
        </ul>
      </div>
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">🌓 Default Appearance</h3>
      <p className="sb-section-item-paragraph">
        Set the default color scheme for your application:
      </p>
      ```tsx
      defaultAppearance: 'system'  // Respects user's OS preference
      ```
      <div style={{ marginTop: '16px' }}>
        <strong>Options:</strong>
        <ul style={{ marginTop: '8px' }}>
          <li><code>light</code> - Always start in light mode</li>
          <li><code>dark</code> - Always start in dark mode</li>
          <li><code>system</code> - Follow user's OS preference (recommended)</li>
        </ul>
      </div>
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">✨ Panel Background</h3>
      <p className="sb-section-item-paragraph">
        Configure the background style for cards, modals, and other panel components:
      </p>
      ```tsx
      panelBg: 'translucent'  // Adds backdrop blur effect
      ```
      <div style={{ marginTop: '16px' }}>
        <strong>Options:</strong>
        <ul style={{ marginTop: '8px' }}>
          <li><code>solid</code> - Solid background color (default)</li>
          <li><code>translucent</code> - Semi-transparent with 10px backdrop blur</li>
        </ul>
      </div>
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">🔤 Typography</h3>
      <p className="sb-section-item-paragraph">
        Define font families for different text elements:
      </p>
      ```tsx
      fontHeading: '"Inter", ui-sans-serif, system-ui, sans-serif',
      fontBody: '"Inter", ui-sans-serif, system-ui, sans-serif',
      fontMono: '"JetBrains Mono", ui-monospace, monospace',
      fontSerif: '"Playfair Display", ui-serif, Georgia, serif'
      ```
      <div style={{ marginTop: '16px' }}>
        <strong>Usage:</strong>
        <ul style={{ marginTop: '8px' }}>
          <li><code>fontHeading</code> - Used for H1, H2, H3, H4, H5, H6 elements</li>
          <li><code>fontBody</code> - Used for paragraphs, spans, and most text</li>
          <li><code>fontMono</code> - Used for code, pre, and kbd elements</li>
          <li><code>fontSerif</code> - Available as <code>font-serif</code> utility class</li>
        </ul>
      </div>
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">🎯 Semantic Colors</h3>
      <p className="sb-section-item-paragraph">
        Configure colors for success, warning, error, and info states:
      </p>
      ```tsx
      // Using Tailwind colors
      successColor: 'green',
      warningColor: 'amber', 
      errorColor: 'red',
      infoColor: 'blue',

      // Or custom color scales (same format as primaryColor)
      successColor: {
        50: '#f0fdf4',
        // ... all shades
        950: '#052e16'
      }
      ```
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">📁 Configuration File Options</h3>
      <p className="sb-section-item-paragraph">
        AtomicFlow looks for theme configuration in these files (in order):
      </p>
      <ul>
        <li><code>atomicflow.config.js</code></li>
        <li><code>atomicflow.config.ts</code></li>
        <li><code>atomicflow.config.mjs</code></li>
        <li><code>theme.config.js</code></li>
        <li><code>theme.config.ts</code></li>
      </ul>
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">💻 TypeScript Configuration</h3>
      <p className="sb-section-item-paragraph">
        For the best development experience, use TypeScript with full type safety:
      </p>
      ```tsx
      // atomicflow.config.ts
      import type { ThemeConfig, ColorScale } from 'atomicflow';

      // Define custom colors with full type safety
      const brandBlue: ColorScale = {
        50: '#eff6ff',
        100: '#dbeafe',
        200: '#bfdbfe',
        300: '#93c5fd',
        400: '#60a5fa',
        500: '#3b82f6',  // Main brand color
        600: '#2563eb',
        700: '#1d4ed8',
        800: '#1e40af',
        900: '#1e3a8a',
        950: '#172554'
      };

      const config: ThemeConfig = {
        primaryColor: brandBlue,     // Full autocomplete
        neutralColor: 'slate',       // IntelliSense suggestions
        defaultRadius: 'lg',         // Type-safe options
        defaultAppearance: 'system',
        panelBg: 'translucent',
        fontHeading: '"Inter", sans-serif',
        fontBody: '"Inter", sans-serif'
      };

      export default config;
      ```
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">🔍 Complete Example</h3>
      <p className="sb-section-item-paragraph">
        Here's a comprehensive theme configuration showcasing all available options:
      </p>
      ```tsx
      // atomicflow.config.ts
      import type { ThemeConfig } from 'atomicflow';

      const config: ThemeConfig = {
        // Brand colors (using custom scale)
        primaryColor: {
          50: '#f0f9ff',
          100: '#e0f2fe',
          200: '#bae6fd',
          300: '#7dd3fc',
          400: '#38bdf8',
          500: '#0ea5e9',
          600: '#0284c7',
          700: '#0369a1',
          800: '#075985',
          900: '#0c4a6e',
          950: '#082f49'
        },
        
        // Neutral colors (using Tailwind)
        neutralColor: 'zinc',
        
        // Typography
        fontHeading: '"Cal Sans", "Inter", sans-serif',
        fontBody: '"Inter", ui-sans-serif, system-ui, sans-serif',
        fontMono: '"JetBrains Mono", ui-monospace, monospace',
        fontSerif: '"Playfair Display", ui-serif, Georgia, serif',
        
        // Appearance
        defaultAppearance: 'system',
        defaultRadius: 'lg',
        panelBg: 'translucent',
        
        // Semantic colors
        successColor: 'emerald',
        warningColor: 'amber',
        errorColor: 'red',
        infoColor: 'sky'
      };

      export default config;
      ```
    </div>
  </div>

<hr style={{ margin: '16px 0' }} />
  <div className="sb-section">
    <div className="sb-section-item">
      <h3 className="sb-section-item-heading">🔧 Troubleshooting</h3>
      <p className="sb-section-item-paragraph">
        Common issues and solutions:
      </p>
      <div style={{
        background: '#fff3cd',
        border: '1px solid #ffeaa7',
        borderRadius: '8px',
        padding: '16px',
        margin: '16px 0',
        color: '#000'
      }}>
        <strong>Theme not updating?</strong>
        <ul style={{ marginTop: '8px', marginBottom: 0 }}>
          <li>Make sure your config file is in the project root</li>
          <li>Restart your development server</li>
          <li>Check the console for validation errors</li>
        </ul>
      </div>
      <div style={{
        background: '#f8d7da',
        border: '1px solid #f5c6cb',
        borderRadius: '8px',
        padding: '16px',
        margin: '16px 0',
        color: '#000'
      }}>
        <strong>Validation errors?</strong>
        <ul style={{ marginTop: '8px', marginBottom: 0 }}>
          <li>Ensure custom color scales have all shades (50-950)</li>
          <li>Use valid Tailwind color names</li>
          <li>Check that all required properties are provided</li>
        </ul>
      </div>
    </div>
  </div>
</div>
