import { Meta } from "@storybook/addon-docs/blocks";

export const RightArrow = () => <svg 
    viewBox="0 0 14 14" 
    width="8px" 
    height="14px" 
    style={{ 
      marginLeft: '4px',
      display: 'inline-block',
      shapeRendering: 'inherit',
      verticalAlign: 'middle',
      fill: 'currentColor',
      'path fill': 'currentColor'
    }}
>
  <path d="m11.1 7.35-5.5 5.5a.5.5 0 0 1-.7-.7L10.04 7 4.9 1.85a.5.5 0 1 1 .7-.7l5.5 5.5c.2.2.2.5 0 .7Z" />
</svg>

<Meta title="Documentation/Installation" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Installation Guide

    Get started with AtomicFlow in your React project in just a few simple steps.
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">📋 Prerequisites</h4>
      <p className="sb-section-item-paragraph">
        Before installing AtomicFlow, make sure your project meets these requirements:
      </p>
      <ul>
        <li><strong>React</strong> 19.0.0 or higher</li>
        <li><strong>TypeScript</strong> 5.0+ (recommended, but JavaScript works too)</li>
        <li><strong>Node.js</strong> 18+ for development</li>
      </ul>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">📦 Step 1: Install the Package</h4>
      <p className="sb-section-item-paragraph">
        Install AtomicFlow using your preferred package manager:
      </p>
      <div style={{ marginBottom: '16px' }}>
        <strong>npm:</strong>
        <pre style={{
          background: '#f6f8fa',
          padding: '16px',
          borderRadius: '8px',
          fontSize: '14px',
          overflow: 'auto',
          margin: '8px 0'
        }}>
          <code>npm install atomicflow</code>
        </pre>
      </div>
      <div style={{ marginBottom: '16px' }}>
        <strong>yarn:</strong>
        <pre style={{
          background: '#f6f8fa',
          padding: '16px',
          borderRadius: '8px',
          fontSize: '14px',
          overflow: 'auto',
          margin: '8px 0'
        }}>
          <code>yarn add atomicflow</code>
        </pre>
      </div>
      <div>
        <strong>pnpm:</strong>
        <pre style={{
          background: '#f6f8fa',
          padding: '16px',
          borderRadius: '8px',
          fontSize: '14px',
          overflow: 'auto',
          margin: '8px 0'
        }}>
          <code>pnpm add atomicflow</code>
        </pre>
      </div>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">🎨 Step 2: Import Styles</h4>
      <p className="sb-section-item-paragraph">
        Import the AtomicFlow styles in your main CSS file or at the root of your application:
      </p>
      <pre style={{
        background: '#f6f8fa',
        padding: '16px',
        borderRadius: '8px',
        fontSize: '14px',
        overflow: 'auto'
      }}>
        <code>{`/* In your main CSS file (e.g., src/index.css or src/App.css) */
@import 'atomicflow/index.css';`}</code>
      </pre>
      <p className="sb-section-item-paragraph">
        <strong>Or</strong> import it directly in your main JavaScript/TypeScript file:
      </p>
      <pre style={{
        background: '#f6f8fa',
        padding: '16px',
        borderRadius: '8px',
        fontSize: '14px',
        overflow: 'auto'
      }}>
        <code>{`// In your src/main.tsx or src/index.tsx
import 'atomicflow/index.css';`}</code>
      </pre>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">⚙️ Step 3: Configure Your Theme (Optional)</h4>
      <p className="sb-section-item-paragraph">
        Create a theme configuration file in your project root to customize the design system:
      </p>
      <pre style={{
        background: '#f6f8fa',
        padding: '16px',
        borderRadius: '8px',
        fontSize: '14px',
        overflow: 'auto'
      }}>
        <code>{`// atomicflow.config.js
/** @type {import('atomicflow').ThemeConfig} */
export default {
  primaryColor: 'blue',           // or custom color scale
  neutralColor: 'slate',          // or custom color scale
  defaultRadius: 'md',            // none, xs, sm, md, lg, xl, 2xl, 3xl, 4xl, full
  defaultAppearance: 'system',    // light, dark, system
  panelBg: 'solid',              // solid, translucent
  fontHeading: '"Inter", sans-serif',
  fontBody: '"Inter", sans-serif',
  successColor: 'green',
  warningColor: 'yellow',
  errorColor: 'red'
};`}</code>
      </pre>
      <p className="sb-section-item-paragraph">
        <em>Note: If you don't create a config file, AtomicFlow will use sensible defaults.</em>
      </p>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">🚀 Step 4: Start Using Components</h4>
      <p className="sb-section-item-paragraph">
        Import and use AtomicFlow components in your React application:
      </p>
      <pre style={{
        background: '#f6f8fa',
        padding: '16px',
        borderRadius: '8px',
        fontSize: '14px',
        overflow: 'auto'
      }}>
        <code>{`import React from 'react';
import { Button, Card, H1 } from 'atomicflow';

export function App() {
  return (
    <div className="p-5">
      <H1>Welcome to AtomicFlow</H1>
      <Card className="mt-5 p-5">
        <p>This card uses your configured theme automatically!</p>
        <Button variant="primary" className="mt-4">
          Get Started
        </Button>
      </Card>
    </div>
  );
}`}</code>
      </pre>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">📚 Framework-Specific Guides</h4>
      <p className="sb-section-item-paragraph">
        AtomicFlow works with any React framework. Here are some popular setups:
      </p>
      
      <div className="mb-6">
        <strong>Next.js 15:</strong>
        <pre style={{
          background: '#f6f8fa',
          padding: '16px',
          borderRadius: '8px',
          fontSize: '14px',
          overflow: 'auto',
          margin: '8px 0'
        }}>
          <code>{`// In your app/globals.css or pages/_app.tsx
import 'atomicflow/index.css';`}</code>
        </pre>
      </div>

      <div className="mb-6">
        <strong>Vite + React:</strong>
        <pre style={{
          background: '#f6f8fa',
          padding: '16px',
          borderRadius: '8px',
          fontSize: '14px',
          overflow: 'auto',
          margin: '8px 0'
        }}>
          <code>{`// In your src/main.tsx
import 'atomicflow/index.css';`}</code>
        </pre>
      </div>

      <div>
        <strong>Create React App:</strong>
        <pre style={{
          background: '#f6f8fa',
          padding: '16px',
          borderRadius: '8px',
          fontSize: '14px',
          overflow: 'auto',
          margin: '8px 0'
        }}>
          <code>{`// In your src/index.css or src/App.css
@import 'atomicflow/index.css';`}</code>
        </pre>
      </div>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">🔧 TypeScript Support</h4>
      <p className="sb-section-item-paragraph">
        AtomicFlow includes full TypeScript support out of the box. For the best experience, create your theme config as a TypeScript file:
      </p>
      <pre style={{
        background: '#f6f8fa',
        padding: '16px',
        borderRadius: '8px',
        fontSize: '14px',
        overflow: 'auto'
      }}>
        <code>{`// atomicflow.config.ts
import type { ThemeConfig } from 'atomicflow';

const config: ThemeConfig = {
  primaryColor: 'emerald',  // Full autocomplete support
  neutralColor: 'zinc',
  defaultRadius: 'lg',
  // ... TypeScript will guide you through all options
};

export default config;`}</code>
      </pre>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">✅ Verification</h4>
      <p className="sb-section-item-paragraph">
        To verify your installation is working correctly, try this simple test:
      </p>
      <pre style={{
        background: '#f6f8fa',
        padding: '16px',
        borderRadius: '8px',
        fontSize: '14px',
        overflow: 'auto'
      }}>
        <code>{`import { Button } from 'atomicflow';

function Test() {
  return (
    <Button onClick={() => alert('AtomicFlow is working!')}>
      Test Button
    </Button>
  );
}`}</code>
      </pre>
      <p className="sb-section-item-paragraph">
        If you see a styled button that shows an alert when clicked, you're all set! 🎉
      </p>
    </div>
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">🆘 Need Help?</h4>
      <p className="sb-section-item-paragraph">
        If you encounter any issues during installation:
      </p>
      <ul>
        <li>Check our <a href="https://github.com/eggnita/atomicflow/issues" target="_blank">GitHub Issues<RightArrow /></a></li>
        <li>Review the <a href="?path=/docs/documentation-theme--docs">Theme Configuration Guide<RightArrow /></a></li>
        <li>Make sure you're using compatible versions of React and TypeScript</li>
      </ul>
    </div>
  </div>
</div>
